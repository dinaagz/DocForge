#!/usr/bin/env bash
# DocForge — unified entry point.
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT="$(cd "$HERE/.." && pwd)"
export DOCFORGE_ROOT="${DOCFORGE_ROOT:-$ROOT}"
export PYTHONPATH="${ROOT}${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m docforge.cli "$@"
