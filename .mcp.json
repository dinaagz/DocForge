{
  "_comment": "MCP servers — OPTIONNEL. DocForge fonctionne sans MCP.",
  "_notes": [
    "Le cœur DocForge ne dépend pas de MCP.",
    "Ce fichier reste pour la compatibilité avec Claude Code.",
    "Le manifeste neutre des connecteurs est dans .docforge/connectors/manifest.yaml.",
    "Les connecteurs marqués OPTIONAL peuvent être absents sans blocage."
  ],
  "mcpServers": {
    "filesystem": {
      "_status": "OPTIONAL",
      "_description": "Filesystem MCP (facultatif — DocForge accède au FS localement).",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem"],
      "env": {"ALLOWED_DIRS": "./input,./output,./work,./state,./.docforge"}
    },
    "github": {
      "_status": "OPTIONAL",
      "_description": "GitHub MCP (facultatif — le CLI git suffit).",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"]
    }
  }
}
